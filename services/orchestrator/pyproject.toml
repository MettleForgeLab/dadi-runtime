[project]
name = "dadi-orchestrator"
version = "0.1.0"
description = "Deterministic pipeline orchestrator with fail-closed validation, artifact storage, lineage graph, and memoization."
requires-python = ">=3.10"
dependencies = [
  "psycopg[binary]>=3.1",
  "jsonschema>=4.18",
  "pydantic>=2.0",
]
