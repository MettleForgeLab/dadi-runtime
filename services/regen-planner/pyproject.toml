[project]
name = "dadi-regeneration-planner"
version = "0.1.0"
description = "Deterministic regeneration planner for DADI-style multi-stage pipelines (Postgres)."
requires-python = ">=3.10"
dependencies = [
  "psycopg[binary]>=3.1",
  "fastapi>=0.110",
  "uvicorn>=0.24",
  "pydantic>=2.0",
]
